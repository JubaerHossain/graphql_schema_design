type Query {
  products: [Product]
  productByPrice(min: Float!, max: Float): [Product]
}
type Product {
  id: ID!
  title: String!
  description: String
  price: Float!
  reviews: [Review]
}
type Review {
  rating: Int
  comment: String
}
